// Write a program to calculate the volume of a cylinder. Take the radius and
//  height as inputs and use the formula:
//  Volume = π * radius^2 * height.
using System;
class Program {
    static void Main() {
        int a=10,b= 20;
        Console.WriteLine(Math.PI*a*a*b);
    }
}
